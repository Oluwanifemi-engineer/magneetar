# ═══════════════════════════════════════════════════════════════════════════════
# MAGNEETAR SERVER DEPENDENCIES
# ═══════════════════════════════════════════════════════════════════════════════

# Core (relaxed pins for Python 3.14 compatibility)
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
python-dotenv>=1.0.0

# Authentication
PyJWT>=2.8.0
cryptography>=42.0.0

# HTTP Client (for alerts)
httpx>=0.28.0

# Database
# sqlite3 is built-in

# PostgreSQL support (required for production Docker deployment)
asyncpg>=0.30.0

# Monitoring & Error Tracking
sentry-sdk>=2.0.0

# Push Notifications (optional, for production)
firebase-admin>=6.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=1.0.0
httpx  # Reuse for test client
