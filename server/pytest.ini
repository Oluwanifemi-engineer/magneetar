[pytest]
markers =
    slow: marks tests as slow (e.g., real WebSocket connections). Skipped in CI via -k "not slow".
filterwarnings =
    ignore::DeprecationWarning:fastapi.*
    ignore::DeprecationWarning:starlette.*
