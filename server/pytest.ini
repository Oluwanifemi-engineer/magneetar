[pytest]
markers =
    slow: live WebSocket integration tests (real uvicorn server + websockets client).
          Run with the full suite — no longer excluded in CI.
filterwarnings =
    ignore::DeprecationWarning:fastapi.*
    ignore::DeprecationWarning:starlette.*
