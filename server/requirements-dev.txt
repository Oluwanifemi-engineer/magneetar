# ═══════════════════════════════════════════════════════════════════════════════
# MAGNEETAR DEV / CI TOOLING
# Quality-gate tooling used by CI, pre-commit, and direct developer runs.
#
# Versions are pinned to match the pre-commit hook environment
# (.pre-commit-config.yaml) so CI, pre-commit, and local runs NEVER drift apart.
# Install alongside runtime deps:  pip install -r requirements.txt -r requirements-dev.txt
# ═══════════════════════════════════════════════════════════════════════════════

# Linting (pinned to the pre-commit flake8 hook rev + its bugbear plugin)
flake8==7.2.0
flake8-bugbear==25.11.29

# Formatter (pinned to the pre-commit black hook rev; needed by `make server-format`)
# isort is deliberately NOT here — pre-commit is its only gate.
black==25.1.0

# Dead-code detection (CI advisory step)
vulture==2.16

# Local pre-commit gate runner
pre-commit==4.6.1
