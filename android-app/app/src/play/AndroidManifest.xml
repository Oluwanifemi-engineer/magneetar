<?xml version="1.0" encoding="utf-8"?>
<!-- ─── Play Store flavor overlay ────────────────────────────────────────────
     Strips permissions and services that trigger Play Protect blocking.
─────────────────────────────────────────────────────────────────────────── -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- SMS permissions (restricted by Play policy) -->
    <uses-permission
        android:name="android.permission.RECEIVE_SMS"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.SEND_SMS"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.READ_PHONE_STATE"
        tools:node="remove" />
    <uses-feature
        android:name="android.hardware.telephony"
        tools:node="remove" />

    <application>
        <!-- Accessibility Service (Play Store rejects non-accessibility usage) -->
        <service
            android:name="com.magneetar.app.UninstallGuardService"
            tools:node="remove" />
    </application>

</manifest>
