<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!--
      Bold solid M — white, thick rounded letterform with a down-dipping V,
      matching the web brand mark.

      GEOMETRY (verified): the ink bounding box (stroke 12, round caps extend
      half the width) is x[28,80] y[32,76] — centre exactly (54,54), the
      adaptive-icon viewport centre. Previous path (M40,69L40,37L60,61L80,37L80,69)
      had its centre at (60,53): 6 units RIGHT — a visible horizontal shift on
      the launcher. All ink stays within the 66dp safe zone (worst distance ~30 < 33).
    -->
    <path
        android:fillColor="#00000000"
        android:strokeColor="#FFFFFF"
        android:strokeWidth="12"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:pathData="M34,70L34,38L54,66L74,38L74,70" />
</vector>
