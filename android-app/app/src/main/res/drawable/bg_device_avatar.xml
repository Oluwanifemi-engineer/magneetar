<?xml version="1.0" encoding="utf-8"?>
<!-- Device avatar tile — dark elevated surface, matching the Stitch design's
     neutral device tile (icon carries the meaning, surface stays quiet). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/md_theme_surfaceVariant" />
    <corners android:radius="12dp" />
    <stroke android:width="1dp" android:color="@color/md_theme_outline" />
</shape>
