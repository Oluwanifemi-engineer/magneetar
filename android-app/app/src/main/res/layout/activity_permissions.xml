<?xml version="1.0" encoding="utf-8"?>
<!-- ══════════════════════════════════════════════════════════════════════
     Permissions wizard — restyled to design/stitch/03-permissions.png.

     Porting contract (same as fragment_home.xml):
       • Every android:id is preserved — PermissionsActivity.kt untouched.
       • Token colors only; no emoji glyphs.
       • The wizard keeps its one-step-per-permission flow; the design's
         list-of-rows is realized by the step structure.
     ══════════════════════════════════════════════════════════════════════ -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:padding="24dp">

    <!-- Step counter -->
    <TextView
        android:id="@+id/tv_step_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Step 1 of 6"
        android:textColor="@color/text_secondary"
        android:textSize="12sp"
        android:textStyle="bold"
        android:letterSpacing="0.1"
        android:layout_marginTop="48dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <!-- Progress bar -->
    <ProgressBar
        android:id="@+id/progress_steps"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="0dp"
        android:layout_height="4dp"
        android:layout_marginTop="12dp"
        android:layout_marginStart="16dp"
        android:progressDrawable="@drawable/progress_teal"
        app:layout_constraintTop_toBottomOf="@id/tv_step_count"
        app:layout_constraintStart_toEndOf="@id/tv_step_count"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Icon -->
    <ImageView
        android:id="@+id/iv_step_icon"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_marginTop="40dp"
        android:padding="16dp"
        android:background="@drawable/bg_icon_ring"
        android:src="@android:drawable/ic_menu_mylocation"
        android:tint="@color/accent_aubergine"
        android:contentDescription="@string/cd_step_icon"
        app:layout_constraintTop_toBottomOf="@id/progress_steps"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Title -->
    <TextView
        android:id="@+id/tv_step_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Location Access"
        android:textColor="@color/text_primary"
        android:textSize="22sp"
        android:textStyle="bold"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/iv_step_icon"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Description -->
    <TextView
        android:id="@+id/tv_step_description"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textColor="@color/text_secondary"
        android:textSize="15sp"
        android:lineSpacingExtra="4dp"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/tv_step_title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Why this matters — icon card (icon replaces the retired emoji) -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="16dp"
        android:padding="12dp"
        android:background="@drawable/bg_card_subtle"
        app:layout_constraintTop_toBottomOf="@id/tv_step_description"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:src="@drawable/ic_tile_lock"
            android:tint="@color/accent_aubergine"
            android:contentDescription="@string/cd_step_icon"/>

        <TextView
            android:id="@+id/tv_step_why"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:textColor="@color/text_secondary"
            android:textSize="13sp"/>
    </LinearLayout>

    <!-- Status indicator -->
    <LinearLayout
        android:id="@+id/layout_status"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:visibility="gone"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/tv_step_description"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/tv_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="@color/status_online"/>
    </LinearLayout>

    <!-- Denied settings redirect -->
    <LinearLayout
        android:id="@+id/layout_denied"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone"
        android:padding="16dp"
        android:background="@drawable/bg_card_subtle"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/tv_step_description"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/perm_denied_hint"
            android:textColor="@color/text_secondary"
            android:textSize="14sp"
            android:gravity="center"/>

        <Button
            android:id="@+id/btn_open_settings"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:layout_marginTop="8dp"
            android:text="@string/perm_open_settings"
            android:textColor="@color/accent_aubergine"
            android:textSize="13sp"
            android:backgroundTint="@android:color/transparent"/>
    </LinearLayout>

    <!-- Buttons -->
    <Button
        android:id="@+id/btn_allow"
        android:layout_width="0dp"
        android:layout_height="52dp"
        android:layout_marginBottom="12dp"
        android:stateListAnimator="@null"
        android:textColor="@color/text_primary"
        android:textSize="15sp"
        android:textStyle="bold"
        android:background="@drawable/btn_primary"
        app:layout_constraintBottom_toTopOf="@id/btn_skip"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <Button
        android:id="@+id/btn_skip"
        android:layout_width="0dp"
        android:layout_height="44dp"
        android:stateListAnimator="@null"
        android:textColor="@color/text_secondary"
        android:textSize="13sp"
        android:background="@drawable/btn_secondary"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="16dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>
