<?xml version="1.0" encoding="utf-8"?>
<!-- Map tab — osmdroid with dual basemaps (street / Esri World Imagery
     satellite) and Esri reference overlays for street paths + place labels.
     Zoom buttons bottom-right, satellite toggle bottom-left. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/md_theme_background">

    <org.osmdroid.views.MapView
        android:id="@+id/osm_map"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <ProgressBar
        android:id="@+id/progress_map"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_gravity="center"
        android:indeterminateTint="@color/md_theme_primary"
        android:visibility="gone" />

    <!-- Zoom controls (bottom-right column) -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:orientation="vertical">

        <ImageButton
            android:id="@+id/btn_zoom_in"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/bg_device_avatar"
            android:src="@drawable/ic_zoom_in"
            android:tint="@color/text_secondary"
            android:contentDescription="@string/cd_zoom_in"
            android:clickable="true"
            android:focusable="true"/>

        <ImageButton
            android:id="@+id/btn_zoom_out"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginTop="8dp"
            android:background="@drawable/bg_device_avatar"
            android:src="@drawable/ic_zoom_out"
            android:tint="@color/text_secondary"
            android:contentDescription="@string/cd_zoom_out"
            android:clickable="true"
            android:focusable="true"/>
    </LinearLayout>

    <!-- Satellite toggle (bottom-left) -->
    <TextView
        android:id="@+id/btn_satellite"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_gravity="bottom|start"
        android:layout_margin="16dp"
        android:gravity="center"
        android:paddingHorizontal="14dp"
        android:background="@drawable/bg_device_avatar"
        android:text="SAT"
        android:textColor="@color/text_secondary"
        android:textSize="12sp"
        android:textStyle="bold"
        android:letterSpacing="0.08"
        android:contentDescription="@string/cd_satellite_toggle"
        android:clickable="true"
        android:focusable="true"/>

</FrameLayout>
